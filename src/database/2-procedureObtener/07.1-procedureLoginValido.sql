use todipelis;

DELIMITER //

CREATE PROCEDURE procedureLoginValido(IN correoElectronico VARCHAR(255), claveAccesso VARCHAR(50))
BEGIN
    DECLARE idUsuarioRetornado INT;
    -- codigo
END//

DELIMITER ;